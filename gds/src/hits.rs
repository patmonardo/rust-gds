/// HITS algorithm result.
#[derive(Debug, Clone)]
pub struct HitsResult {
    // TODO: Add fields as needed
}

impl HitsResult {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for HitsResult {
    fn default() -> Self {
        Self::new()
    }
}
