/// Logging infrastructure for GDS.
#[derive(Debug, Clone)]
pub struct Log {
    // TODO: Add fields as needed
}

impl Log {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Log {
    fn default() -> Self {
        Self::new()
    }
}
