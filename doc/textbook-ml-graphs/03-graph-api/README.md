## 03 — Graph API (Projection & Traversal)

Projection model and traversal patterns.

**Examples** (in `archive/examples/03-graph-api/`):
- projection_showcase.rs — ✅ Node/Relationship projections
- graphstore_graph_api_exploration.rs — ✅ Graph API tour
- relationship_cursor_traversal.rs — ✅ Cursor-based traversal
- relationship_property_filtered_view.rs — ✅ Filtered views
- node_value_access.rs — ✅ Property access on views
- traversal_inspector.rs — ✅ Inspect traversal

**Key Concepts**:
- Projection-based graph modeling
- Cursor-based traversal patterns
- Filtered views and property access
